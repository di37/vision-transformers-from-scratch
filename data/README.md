# Data

This directory stores the datasets used for training and evaluation.

- **CIFAR-100**: The scripts are configured to automatically download the CIFAR-100 dataset into this directory if it is not present.
